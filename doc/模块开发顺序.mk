模块开发顺序：
核心原则：**从底层基础 → 数据结构 → 核心服务 → UI绑定**，严格遵循**单向依赖**，**每一步只修改一个模块**，完全避免交叉修改、循环依赖、开发阻塞。

# 🚀 最终开发顺序（6个阶段，循序渐进）
## 阶段 1：开发 **工具层（utils）** → 全局基础底座
**优先级：最高**
**开发文件**：
`utils/config.py` + `utils/common.py`
**核心内容**：
1. 全局配置（窗口大小、data目录路径、ADB命令参数、项目名称）
2. 通用工具（路径创建、文件读写、时间格式化）
**为什么先做？**
整个项目的**公共基础**，所有后续模块都依赖配置和工具，一次性写完，全程不再修改。

---

## 阶段 2：开发 **模型层（model）** → 统一数据格式
**优先级：极高**
**开发文件**：
`model/device_model.py` + `model/test_model.py`
**核心内容**：
1. 设备数据结构（USB序列号、连接状态、固件版本等）
2. 测试数据结构（用例信息、测试结果、报告数据）
**为什么第二步？**
纯数据结构，**无逻辑、无依赖**，定义好后，服务层/UI层的数据传递完全标准化，彻底杜绝混乱。

---

## 阶段 3：开发 **核心基础服务（service）** → 硬件+日志（项目根基）
**优先级：高**
**开发顺序**：
1. `service/log_service.py`（日志服务）→ 写入 `data/logs`
2. `service/adb_service.py`（USB-ADB服务）→ 设备扫描、命令执行
**核心内容**：
- 日志：全项目日志统一输出
- ADB：USB设备识别、执行shell命令（测试工具的**核心硬件能力**）
**为什么第三步？**
ADB是你的工具**最核心功能**，日志是全项目调试基础，先验证硬件连通性，后续业务才有意义。

---

## 阶段 4：开发 **业务服务（service）** → 测试+报告+文件
**优先级：中高**
**开发顺序**：
1. `service/test_service.py`（测试引擎）→ 基于ADB执行测试用例
2. `service/report_service.py`（报告服务）→ 生成报告到 `data/reports`
3. `service/file_service.py`（文件服务）→ 固件管理
**核心内容**：
实现所有**业务逻辑**，完全独立于UI，只依赖底层服务/模型/工具。
**优势**：纯逻辑开发，不碰UI，不碰界面，单一模块修改。

---

## 阶段 5：UI 与服务 **绑定（唯一的界面交互）**
**优先级：中**
**开发文件**：
`ui/` 下所有标签页 + `main.py`
**核心内容**：
1. 按钮点击 → 调用对应服务（如：刷新设备 → 调用 adb_service）
2. 服务返回结果 → 更新UI（如：设备列表、日志、报告）
**关键规则**：
**UI 只调用服务，不写任何业务逻辑！** 完全符合解耦设计。

---

## 阶段 6：整体联调 + 功能完善
**优先级：低**
**内容**：
1. 全流程测试（USB连接 → 扫描设备 → 执行测试 → 生成日志/报告）
2. BUG修复、细节优化
**优势**：架构已定型，仅修复细节，**不修改模块结构**。

---

# 🎯 最简总结（记忆版）
```
工具层 → 模型层 → 日志/ADB服务 → 测试/报告服务 → UI绑定 → 联调
```

# 🔥 这个顺序的核心优势（完全匹配你的需求）
1. **解耦落地**：每一步只改一个模块，绝不修改多个文件
2. **无开发阻塞**：底层先完成，上层直接调用，不用等待
3. **风险最低**：先验证核心的USB-ADB硬件功能，再做业务
4. **结构稳定**：全程不改动架构，不新增/删除文件
5. **数据统一**：日志/报告全部自动存入 `data/` 目录

---

# 下一步建议
直接从 **阶段1** 开始：
先写 `utils/config.py` 全局配置文件，这是最简单、最基础、最无风险的第一步！