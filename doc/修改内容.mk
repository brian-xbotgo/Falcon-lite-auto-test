fix: 修复文件管理和部分测试页面bug

✅ 修复内容:
1. 文件管理页面
   - 移除重复绑定的itemDoubleClicked信号
   - 修复双击打开文件需要关闭两次的问题

2. 部分测试页面
   - 修改单步执行逻辑：改为执行选中的测试用例
   - 不再按顺序自动执行下一个
   - 支持选择任意测试用例单独测试
   - 添加未选中时的提示信息

✅ 无架构破坏，仅修改UI交互逻辑，核心引擎和架构完全不变



当前文件系统部分内容是按照变色龙来写的，后续需要修改：
1.蓝牙版本号读取逻辑
2.adb devices读取的设备号为proc/cpuinfo
3.



阅读doc下的架构规范、上位机项目架构了解项目架构,现在我要重构一下设备扫描与连接的方案
你先不用修改代码，我们先来做一下初步的规划
1.目前我们的逻辑是，我在tab_all_test点击btn_connect_adb和btn_disconnect_adb来进行连接，这个方案目前有点问题
2.首先是对这个设备连接ui显示区域进行修改，整体高度增加（注意页面整体别超出或者允许略微增加这个页面的高度最多允许把config.py的WINDOW_HEIGHT增加25-50），然后拆分成两部分，上半部分是蓝牙扫描到的设备名称，呈现出一个列表（列表只有两列，第一行固定的情况下可以滚动向下查看，设备名称与连接状态，选中设备后（选中记得高亮）可以点击连接按钮），已连接的设备置顶到表格顶部，同时将btn_connect_adb和btn_disconnect_adb挪动到合理的位置
3.下半部分显示的是当前连接的设备，整体是原先的表格头部加一列为设备名称，连接成功后这个表格会显示，总共为两行，一行表头一行设备内容，然后删除掉root权限列，同时删除掉adb_service.py中与root相关的函数与调用，DeviceModel也需要同步修改成员
4.该区域要再新增一个刷新按键，每次点刷新都进行一次扫描（每次都更新整个列表，如果设备很多的话可以滚动下滑保证表头不动）
5.三个按钮都集成到上半区进行合理布局
6.新增蓝牙扫描模块，这个模块是扫描过滤名称为Xb开头的蓝牙设备，然后把扫描出来Xb开头的设备都添加到设备连接的显示区，并且在当前的表格中新增一列为设备名称，该列显示扫描出来Xb开头的设备名，设备名就是蓝牙包中的[Complete Local Name] 项，这个蓝牙扫描的代码写在哪个位置要进行分析一下。不能和测试用例混在一起
7.刷新扫描出设备，点击连接后，原先走的是adb_service.py中的scan_devices,这里的连接是通过adb devices扫描出设备号然后根据这个设备号进行连接，我现在的想法是我通过这个连接后，这个设备名称有两种不同的形式，（第一种是从/oem/usr/bin/cpuinfo.txt读取CPU序列号
计算CRC24校验和，取后3字节转6位十六进制，得到格式: XbotGo-XXXXXX (例如: XbotGo-a1b2c3)；第二种是通过uuid=$(cat /userdata/cpuinfo.txt | sha256sum | tail -c 6) ，ssid="Xbt-F-${uuid}"，得到的格式是"Xbt-F-xxxxxx"）我这里需要增加一个检验，如果将读取到的蓝牙设备名称与这两个路径转换路径中的内容进行比对（这里写成两个if判断，到时候我会注释掉其中一种来使用），如果相同则连接成功，如果不同则执行关闭adb连接
8.adb_service.py中需要新增一个函数为adb_pull_file()，这个函数会传两个参数，一个参数是设备中的路径，另一个是本机上的路径，主要功能是像在powershell执行adb pull从设备上拉取文件下来，至于返回值可以设计一下，要注意格式
9.如果处于连接状态时USB线断掉，应该要能够及时显示设备断开
接下来你开始分析一下我的方案是否合理且是否有漏洞，是否有哪些文件没有进行修改，然后开始规划，要告诉我有哪些文件需要修改等


1.应该是不需要root权限，我测试过在powershell执行pull ... ...是可以运行的
2.放弃CRC24校验，我确认了这个设备应该是通过第二种方式得到设备名称，校验部分只需要生成这个，但是这个校验部分生成后先注释掉因为现在只有第一种方案的设备，然后设备的蓝牙广播我研究了一下，第一种设备是用杰理AC6925芯片发出的BLE广播，第二种设备是Linux BlueZ发出的BLE广播，第二种设备是这个项目的核心设备，第一种设备是我目前只能用到的测试设备，所以蓝牙库依赖你觉得应该用哪种，后续整个项目都会围绕第二种设备来运作
3.暂时不考虑多设备连接
继续确认一下方案，如果需要安装什么库你把pip的命令给我我来下载