# RV1126B ADB 自动化冒烟测试工具 - 最终整合版项目设计与架构
**版本**：V1.0（2026-04-13 定稿）
**核心原则**：分层解耦、高内聚低耦合、USB有线ADB专属、日志/报告统一归档、单一模块修改无交叉

## 一、项目概述
### 1.1 项目名称
RV1126B ADB 自动化冒烟测试工具

### 1.2 项目定位
面向 RV1126B 芯片设备的**USB有线ADB自动化测试上位机**，支持批量自动化测试+人工集中确认、实时日志、测试报告生成、文件管理，全程可追溯、结果可导出。

### 1.3 核心功能
1. USB有线ADB设备扫描、连接、指令执行
2. 全量冒烟测试/部分功能调试双模式
3. 自动化测试优先 + 人工项集中确认
4. 实时分级日志输出，自动保存至本地
5. 测试报告自动生成（Excel/HTML）
6. 固件/日志/报告统一文件管理

### 1.4 技术栈
- 开发语言：Python 3.10+
- UI框架：PyQt6
- 通信方式：USB ADB（subprocess 封装）
- 数据存储：本地 `data/` 目录
- 报告生成：Excel/HTML
- 架构模式：分层解耦架构（无MVC强绑定）

## 二、整体架构设计
### 2.1 分层架构（5层单向依赖，无循环、无跨层调用）
```
UI 展示层 → 业务服务层 → 数据模型层 → 通用工具层 → 数据持久层
```
- **依赖规则**：仅上层调用下层，下层绝不感知上层
- **核心目标**：修改单一模块无需改动其他文件，彻底解耦

### 2.2 分层职责边界
| 层级 | 核心职责 | 禁止操作 |
|------|----------|----------|
| **UI 展示层** | 纯界面渲染、按钮响应、数据展示 | 不写任何ADB/测试/文件业务逻辑 |
| **业务服务层** | 全项目业务逻辑（ADB、测试、日志、报告） | 不直接操作UI控件 |
| **数据模型层** | 定义统一数据结构（设备、测试用例、结果） | 不处理逻辑、不操作UI |
| **通用工具层** | 全局通用能力（配置、路径、时间、公共方法） | 不绑定具体业务 |
| **数据持久层** | 唯一数据出口（日志、报告、固件存储） | 不存放代码/配置文件 |

## 三、最终定型目录结构
```
RV1126B_Test_Tool/          # 项目根目录
├── main.py                 # 唯一程序入口：初始化APP + 启动主窗口
├── ui/                     # UI展示层（纯界面，0业务逻辑）
│   ├── __init__.py
│   ├── main_window.py      # 主窗口框架：整合所有标签页
│   ├── tab_all_test.py     # 全部测试页面UI
│   ├── tab_part_test.py    # 部分功能测试页面UI
│   ├── tab_log_view.py     # 日志查看页面UI
│   └── tab_file_manager.py # 文件管理页面UI
├── service/                # 业务服务层（核心解耦层，所有功能入口）
│   ├── __init__.py
│   ├── adb_service.py      # ADB服务：仅负责USB设备连接/命令执行
│   ├── test_service.py     # 测试服务：测试用例执行/流程控制
│   ├── log_service.py      # 日志服务：日志生成+写入data文件夹
│   └── report_service.py   # 报告服务：测试报告生成+存储
├── model/                  # 数据模型层：纯数据结构，无逻辑
│   ├── __init__.py
│   ├── device_model.py     # 设备模型：USB设备信息（序列号/状态等）
│   └── test_model.py       # 测试模型：用例/结果/报告数据结构
├── utils/                  # 工具层：全局通用工具，无业务
│   ├── __init__.py
│   ├── config.py           # 全局配置：窗口尺寸、ADB参数、数据路径等
│   └── common.py           # 公共工具：文件操作、格式转换等通用方法
└── data/                   # 数据持久层：所有输出文件统一存放
    ├── logs/               # 运行日志、操作日志
    ├── reports/            # 测试报告（Excel/TXT/HTML）
    └── firmware/           # 固件包存储（用于升级测试）
```

## 四、核心模块定义
### 4.1 UI层模块（纯界面）
| 文件 | 对应页面 | 核心界面组件 |
|------|----------|--------------|
| main_window.py | 主窗口 | 标签页导航、状态栏、菜单栏 |
| tab_all_test.py | 全部测试 | 设备列表、测试项选择、参数配置、测试控制、实时日志、测试用例状态面板（成功/失败/当前/待执行） |
| tab_part_test.py | 部分功能测试 | 测试项树、单步执行、跳过当前项、调试日志 |
| tab_log_view.py | 日志查看 | 日志文件列表、日志预览、删除/导出/刷新 |
| tab_file_manager.py | 文件管理 | 目录树、文件列表、上传/下载/删除/刷新 |

### 4.2 服务层模块（业务核心）
| 文件 | 核心功能 | 依赖 |
|------|----------|------|
| adb_service.py | USB设备扫描、ADB指令执行、设备状态获取 | model/device_model、utils |
| test_service.py | 测试用例调度、自动化/人工流程控制、状态更新 | adb_service、model/test_model |
| log_service.py | 分级日志输出、自动写入data/logs | utils/config |
| report_service.py | 测试报告生成、保存至data/reports | test_model、utils |

### 4.3 模型层模块（数据结构）
| 文件 | 数据定义 |
|------|----------|
| device_model.py | 设备序列号、连接方式、状态、固件版本、Root状态 |
| test_model.py | 测试用例、测试类型、优先级、执行状态、结果、备注 |

### 4.4 工具层模块（通用能力）
| 文件 | 核心能力 |
|------|----------|
| config.py | 全局配置（窗口、路径、ADB参数） |
| common.py | 路径创建、时间格式化、文件读写 |

## 五、数据模型设计
### 5.1 设备模型（DeviceModel）
- 序列号（serial）
- 连接方式（conn_type）：固定USB
- 状态（status）：在线/离线
- 固件版本（version）
- Root状态（is_rooted）

### 5.2 测试模型（TestModel）
- 测试ID（id）
- 模块（module）
- 测试名称（name）
- 测试类型（type）：自动化/人工
- 优先级（priority）：P0/P1/P2
- 执行状态（status）：等待中/执行中/通过/失败/待确认
- 执行耗时（duration）
- 备注（remark）
- 确认人（executor）：人工项专用

## 六、核心业务流程
### 6.1 测试主流程（自动化优先+人工集中确认）
1. 启动工具 → 扫描USB-ADB设备 → 选中设备
2. 勾选测试项（自动化+人工）→ 配置参数
3. 开始测试 → **先执行所有自动化项**
4. 自动化完成 → 自动暂停 → 开始人工确认项
5. 人工逐项确认 → 所有项执行完毕
6. 自动生成日志+测试报告 → 保存至data目录

### 6.2 数据流向
1. UI触发操作 → 调用服务层接口
2. 服务层执行业务 → 读写模型数据
3. 服务层调用工具层 → 操作文件/路径
4. 结果输出 → 写入data目录 → 回调更新UI

## 七、数据存储规范
### 7.1 存储目录（唯一出口）
- 运行日志：`data/logs/YYYYMMDD_XXX_test.log`
- 测试报告：`data/reports/YYYYMMDD_XXX_result.xlsx/html`
- 固件文件：`data/firmware/xxx.img`

### 7.2 存储规则
- 日志带时间戳、分级（INFO/DEBUG/WARNING/ERROR）
- 报告包含测试概览、详细结果、问题汇总
- 所有文件命名唯一、按时间排序

## 八、官方认可开发顺序
### 阶段1：开发工具层（utils）→ 全局基础
- `utils/config.py` + `utils/common.py`

### 阶段2：开发模型层（model）→ 统一数据格式
- `model/device_model.py` + `model/test_model.py`

### 阶段3：开发核心基础服务（service）→ 硬件+日志
1. `service/log_service.py`
2. `service/adb_service.py`

### 阶段4：开发业务服务（service）→ 测试+报告
1. `service/test_service.py`
2. `service/report_service.py`

### 阶段5：UI与服务绑定 → 界面交互
- UI按钮调用服务，服务结果更新UI

### 阶段6：整体联调+完善
- 全流程测试、BUG修复、细节优化

## 九、代码规范
### 9.1 命名规范
- 类名：大驼峰（PascalCase）
- 函数/变量：小写下划线（snake_case）
- 常量：全大写下划线（UPPER_SNAKE_CASE）
- 私有成员：单下划线前缀（_xxx）

### 9.2 格式规范
- 遵循PEP8，使用black自动格式化
- 文件头注释：文件名、功能、作者、时间
- 函数注释：Google风格，说明参数、返回值、异常

### 9.3 解耦黄金规则
1. UI不写业务逻辑，仅调用服务
2. 服务不操作UI，仅通过数据回调
3. 单向依赖，禁止循环导入
4. 一个模块只负责一件事
5. 新增功能不修改旧代码

## 十、项目核心优势
1. **彻底解耦**：修改单一模块无需改动多文件
2. **USB专属**：仅支持有线ADB，无冗余功能
3. **数据统一**：日志/报告/固件统一归档至data
4. **易维护**：分层清晰、职责明确、结构稳定
5. **可扩展**：新增测试项/功能仅需新增模块
6. **鲁棒性**：完善的异常处理、超时重连、日志追溯